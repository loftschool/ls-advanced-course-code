script(type="text/x-template")#animation
  div.root
    button(@click="slide('up')") вверх
    button(@click="slide('down')") вниз
    .list-wrapper
        li.item(v-for="work in filteredArray" :key="work.id")
          .item-wrapper
            img(:src="work.photo", alt="")